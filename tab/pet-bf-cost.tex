\begin{table}[t]
  \UseAltLinespread
  \caption
  [The number of distributions used for a simulated \protect\pet data
    set using the \protect\smc[2] algorithm]
  {The number of distributions used for a simulated \protect\pet data
  set using the \protect\smc[2] algorithm.}
  \label{tab:pet four sampler same iter}
  \begin{tabularx}{\linewidth}{lXX}
    \toprule
    & \multicolumn{2}{c}{Proposal scales} \\
    \cmidrule(lr){2-3}
    & Fixed & Adaptive \\
    \midrule
    Fixed annealing ($\alpha_k(t/T_k) = (t/T_k)^5$) & $200$ & $182$ \\
    \cess-based adaptive annealing & $175$ & $157$ \\
    \bottomrule
    \multicolumn{3}{r}{%
      \begin{minipage}{\linewidth-2em}\vskip1ex\sffamily
        All four samplers are configured such that the standard Bayes factor
        estimates ($\log B_{2,1}\pm\sd$), obtained from 100 simulations for
        each sampler, have a standard deviation of approximately $0.27$.
      \end{minipage}}
  \end{tabularx}
\end{table}
